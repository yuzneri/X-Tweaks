# Snapshots of the real DOM.
# They contain actual post contents and account names, so they stay out of the history.
# Keep them locally when verifying selectors.
test/

# Temporary files created by the Playwright MCP
.playwright-mcp/

# Files created by the OS
.DS_Store
._.DS_Store

# Dependencies
node_modules/

# Build output. `npm run build` regenerates it, and the packages carry it.
dist/

# Packages built for submission
web-ext-artifacts/
